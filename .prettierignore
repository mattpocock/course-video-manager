# Dependencies
node_modules
pnpm-lock.yaml

# Build outputs
build
dist
.react-router

# Database
*.db
*.db-journal
*.db-shm
*.db-wal

# Generated files
.env
.DS_Store
*.log

# Vendored, generated, append-only lucide geometry — never hand-edited, and
# reformatting 400KB of it on every commit helps nobody.
packages/lucide-icons/lib/generated

# Coverage
coverage

# Misc
.vscode
.idea

# Clip Mockup frame examples. Each code/terminal line is one whitespace-
# significant element (`white-space: pre`), and Prettier — which sees only
# inline <span>s — rewraps their contents and injects line breaks INSIDE a
# line of code. The captured frame then shows one token per row.
frame-examples/*.html
